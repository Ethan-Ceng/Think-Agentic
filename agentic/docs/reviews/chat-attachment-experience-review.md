# 聊天附件交互增强代码审查

## 审查范围

- 目标分支：`feature/chat-artifact-preview`
- 变更分支：`feature/chat-attachment-experience`
- 变更范围：附件体验相关代码、测试与文档；基线 `16ccd1e`
- 设计文档：`agentic/docs/designs/chat-attachment-experience.zh-CN.md`
- 计划文档：`agentic/docs/plans/chat-attachment-experience-plan.md`
- 审查者：同一 Agent 自检
- 审查日期：2026-07-26

`85d84f7` 中已存在的延期开发展示/导出设计文档不属于本次附件代码审查；后端、依赖、lockfile 和生成声明已单独做零差异检查。

## 需求符合度

- [x] 统一附件菜单、本地拖拽、“我的文件”多选和安全图片缩略图符合设计。
- [x] 本地选择、粘贴和拖拽共用 `uploadFiles`；文件库附件直接复用持久化 ID。
- [x] 首页、普通 Session 和 running Session 下一条消息沿用 `attachmentIds` 协议。
- [x] Session 切换、迟到请求和 Blob URL 生命周期有明确隔离与测试。
- [x] 未新增后端、数据库、依赖、lockfile、shell、沙箱写入或工具批准能力。

## 正确性

- [x] 目录、搜索、筛选、分页、跨页选择、去重和请求竞态有覆盖。
- [x] 上传、预览和发送的成功、失败、移除、切换与卸载路径有覆盖。
- [x] 文件库预览迟到不会写回已移除或已切换的附件。
- [x] 旧 Session 发送迟到完成不会清除新 Session 附件。
- [x] 空文件投放不拦截默认行为，类型信息丢失的 `dragleave` 仍会清理覆盖层。

## 安全性

- [x] 文件库选择结果只包含白名单元数据，不携带路径、下载 URL、存储 Provider 或用户 ID。
- [x] 只有 MIME 与扩展名共同匹配的安全栅格图片进入 `<img>` 预览；SVG 被排除。
- [x] 文件列表、预览和发送继续使用现有当前用户鉴权与服务端二次查询。
- [x] Blob URL 只存在于内存，不进入消息、草稿、API payload 或日志。

## 可维护性

- [x] 附件归一化与去重规则集中在 `composer-attachments.ts`。
- [x] `ChatComposer` 只负责交互与展示，`ChatInput` 继续拥有上传和附件状态。
- [x] Dialog 复用现有文件 API，未复制 FilesView 的持久化操作。
- [x] 样式集中在独立附件体验样式文件并使用现有亮/暗色 Token。

## 测试质量

- [x] 关键规则采用失败测试先行。
- [x] 覆盖文件/非文件/空文件拖拽、嵌套离开、菜单、图片降级和键盘关闭。
- [x] 覆盖文件库不重复上传、预览迟到、URL 回收、发送失败保留和快速 Session 切换。
- [x] 覆盖首页初始化、普通 Session 和 running queue 附件 ID。
- [x] 最新全量结果为 39 个测试文件、149 项测试通过。

## 问题列表

未发现未处理的 `blocking`、`major`、`minor` 或 `suggestion`。

审查过程中发现并已修复一项 `major`：`drop` 只有 `Files` 标记但无实际文件时会错误拦截默认行为，且类型信息被浏览器清空的 `dragleave` 可能留下覆盖层。修复后新增回归测试，并重新运行全量测试、类型检查和生产构建。

## 无法验证项

无。用户于 2026-07-26 完成桌面、390px、暗色模式、真实文件拖放、Dialog 布局、键盘与焦点流页面验收并确认通过。

## 合并门禁

| 门禁 | 结果 | 证据 |
| --- | --- | --- |
| 无 blocking | 通过 | 完整附件代码自检未发现 blocking |
| 无未处理 major | 通过 | 拖拽 major 已修复并新增回归 |
| 验收标准满足 | 通过 | 自动化规则通过；用户真实页面验收通过 |
| 相关测试通过 | 通过 | 39 个文件、149 项测试通过 |
| 类型检查通过 | 通过 | `pnpm type-check` 退出 0 |
| 构建通过 | 通过 | `pnpm build` 退出 0，3669 个模块完成转换 |
| 数据迁移已验证 | 不适用 | 无后端或数据库变更 |

## 审查结论

- 结论：`APPROVED`
- 理由：自动化、类型、构建、静态边界、用户真实页面验收均通过，无未处理 blocking/major。
- 剩余风险：同一 Agent 自检弱于独立 Reviewer；本批未新增后端、数据库或迁移风险。
- 下一步：等待用户明确要求提交、推送或合并；本审查本身不执行 Git 写操作。
